import TaskListPacket from '../../../packets/TaskListPacket';
import Player from '../../Player';

// skipcq: JS-0356 | Deepsource Unused variable
export default function (player: Player, packet: TaskListPacket): void {
  // Not sending data back to lunar
}
